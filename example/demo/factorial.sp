import "resource/functions";
function factorial(int n)=(int result)
{
        auto result = 1;
        if ('n greater 1)
        {
                result = 'n mul factorial('n sub 1);
        }
}
show(factorial(5));